## 数组

### 什么是数组?

**数组的含义**  
内存中一段类型相同的连续空间。

**数组的定义**  
类型名 + 数组名 + [元素个数]；
	
### 数组的使用

**数组的初始化** 
```cpp
int a[..] = {};
```

**数组的清空**
```cpp
memset(a,0,sizeof(a)); //将数组a中的数据置成0;
``` 

**数组元素的引用**
```cpp 
数组名 + [下标序号]  //！下标默认从 0开始
```
		